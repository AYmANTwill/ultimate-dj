# Ultimate DJ — runtime dependencies
# Auto-installed at first launch by app/deps.py if missing.

# UI
customtkinter>=5.2.0
Pillow>=10.0.0

# Audio analysis + playback
librosa>=0.10.0
numba>=0.66                 # librosa's jit dep — older numba rejects
                            # NumPy 2.2 and kills librosa.load entirely
numpy>=1.24.0
soundfile>=0.12.0
sounddevice>=0.4.6
mutagen>=1.47.0
pyrubberband>=0.3.4         # optional (also needs rubberband.exe on PATH)

# Downloads & 3rd-party APIs
yt-dlp[default]>=2026.0.0
spotipy>=2.23.0
cloudscraper>=1.2.71            # cheap path for static pages
playwright>=1.50.0              # JS-rendered pages (1001tracklists);
                                  # needs `playwright install chromium`
                                  # after pip install (~150 MB download)
playwright-stealth>=2.0         # bot-detection evasion for 1001tracklists
beautifulsoup4>=4.12
lxml>=4.9

# Browser embedding (Windows: WebView2 runtime)
pywebview>=6.0

# Rekordbox bridge — lit master.db (historique de sets) + ANLZ (cues)
# en LECTURE SEULE ; alimente le corpus L2 et le mode Live
pyrekordbox>=0.4

# Secrets / OS integration
keyring>=24.0

# External binaries expected on PATH (installed by deps.py via winget):
#   ffmpeg       — audio conversion (mandatory)
#   node         — yt-dlp YouTube auth (mandatory)
#   rubberband   — high-quality time-stretch (optional)
